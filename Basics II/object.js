const student={
    name: "Yangzoom",
    age: 18,
    grade: "Undergraduate",
    isEnrolled: true
};

console.log(`${student.name} is ${student.age} years old and is currently in ${student.grade}`);